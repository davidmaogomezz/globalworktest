<%= form_with(model: role_user, local: true) do |form| %>
  <% if role_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(role_user.errors.count, "error") %> prohibited this role_user from being saved:</h2>

      <ul>
      <% role_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :role_id %>
    <%= form.text_field :role_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
